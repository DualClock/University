<UserControl x:Class="UniversitySystem.Controls.GradeManagementControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Background="#F8F9FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="📝" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="Ввод оценок" FontSize="20" FontWeight="Bold" Foreground="#333"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <ComboBox x:Name="GroupComboBox" Width="150" Padding="10" DisplayMemberPath="Name" SelectionChanged="GroupChanged"/>
                        <ComboBox x:Name="DisciplineComboBox" Width="150" Margin="10,0,0,0" Padding="10" DisplayMemberPath="Name"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <ComboBox x:Name="SemesterCombo" Width="100" Padding="10">
                            <ComboBoxItem Content="1"/>
                            <ComboBoxItem Content="2"/>
                        </ComboBox>
                        <TextBox x:Name="YearTextBox" Width="100" Margin="10,0,0,0" Padding="10" Text="2024-2025"/>
                        <Button Content="📥 Загрузить" Background="#4A00E0" Foreground="White" Padding="15,8" BorderThickness="0" Margin="10,0,0,0" MinWidth="120" MinHeight="38" Click="LoadGrades_Click"/>
                        <Button Content="💾 Сохранить" Background="#28A745" Foreground="White" Padding="15,8" BorderThickness="0" Margin="10,0,0,0" MinWidth="120" MinHeight="38" Click="SaveGrades_Click"/>
                    </StackPanel>
                    <DataGrid Grid.Row="2" x:Name="GradesDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" Background="Transparent" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Студент" Binding="{Binding FullName}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Роль" Binding="{Binding Role}" Width="80" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Оценка (2-5)" Binding="{Binding TempGrade}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
            <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="20" Margin="20,0,0,0">
                <StackPanel>
                    <TextBlock Text="Настройки оценки" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="Тип" FontSize="12" Foreground="#666" Margin="0,0,0,5"/>
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="ExamRadio" Content="Экзамен" GroupName="GradeType" IsChecked="True" Margin="0,0,15,0"/>
                            <RadioButton x:Name="SeminarRadio" Content="Семинар" GroupName="GradeType" Margin="0,0,15,0"/>
                            <RadioButton x:Name="LabRadio" Content="Лаб. работа" GroupName="GradeType"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Text="Выберите группу и дисциплину, затем нажмите 'Загрузить' для получения списка студентов." FontSize="12" Foreground="#666" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
