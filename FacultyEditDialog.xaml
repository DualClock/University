<Window x:Class="UniversitySystem.FacultyEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Редактирование факультета"
        Height="300"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Форма редактирования -->
        <StackPanel>
            <TextBlock Text="Название факультета" Style="{StaticResource MaterialDesignBody}"/>
            <TextBox x:Name="TxtName" 
                     materialDesign:HintAssist.Hint="Введите название факультета"
                     Style="{StaticResource MaterialDesignTextBox}"
                     Margin="0,0,0,12"/>

            <TextBlock Text="Код факультета" Style="{StaticResource MaterialDesignBody}" Margin="0,0,0,8"/>
            <TextBox x:Name="TxtCode" 
                     materialDesign:HintAssist.Hint="Введите код факультета"
                     Style="{StaticResource MaterialDesignTextBox}"/>
        </StackPanel>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Сохранить" 
                   Click="BtnSave_Click"
                   Style="{StaticResource MaterialDesignRaisedButton}"
                   IsDefault="True"
                   Margin="0,0,8,0"/>
            <Button Content="Отмена" 
                   Click="BtnCancel_Click"
                   Style="{StaticResource MaterialDesignFlatButton}"
                   IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>