<Window x:Class="UniversitySystem.GradesViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Просмотр оценок"
        Height="650"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,15">
            <TextBlock Text="📊" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="Просмотр оценок" FontSize="20" FontWeight="Bold" Foreground="#333"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="20">
                <StackPanel Orientation="Horizontal">
                    <ComboBox x:Name="StudentComboBox" Width="300" DisplayMemberPath="FullName" SelectedValuePath="Id" materialDesign:HintAssist.Hint="Выберите студента" SelectionChanged="Student_SelectionChanged"/>
                    <Button Content="📥 Экспорт в Excel" FontSize="12" Background="#4A00E0" Foreground="White" Padding="15,8" BorderThickness="0" Margin="20,0,0,0" Click="ExportToExcel_Click"/>
                </StackPanel>
            </Border>
            <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" Margin="0,15,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock x:Name="StudentNameText" FontSize="16" FontWeight="Bold" Foreground="#333"/>
                        <TextBlock x:Name="AverageText" FontSize="16" Foreground="#4A00E0" Margin="30,0,0,0"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" x:Name="GradesDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="Transparent" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Дисциплина" Binding="{Binding Discipline.Name}" Width="*"/>
                            <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="100"/>
                            <DataGridTextColumn Header="Оценка" Binding="{Binding Value}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="14"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Семестр" Binding="{Binding Semester}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <TextBlock Text="Средний балл:" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TotalAverageText" FontSize="18" FontWeight="Bold" Foreground="#4A00E0" Margin="10,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
